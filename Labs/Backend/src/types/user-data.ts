// 1. Define o tipo userData com os campos nome, email e senha
type userData = {
  name: string;
  email: string;
  password: string;
};

// 2. Exporta o tipo userData como padrão
export default userData;